# Dossier code

Ce dossier contient le code source du projet SPHIRIT.

Structure suggérée :
- `src/` : code source (modules, scripts)
- `tests/` : tests unitaires
- `examples/` : cas tests et scripts d'exécution (DamBreak, Funnel, ...)

Consignes :
- Placer les scripts Python et les modules dans `src/`.
- Utiliser Python 3.11+ (voir README principal).
- Ajoutez un `README.md` ou une `main.py` dans chaque sous-dossier pour expliquer son contenu.

Ce fichier a été créé automatiquement pour matérialiser le dossier `code`.
